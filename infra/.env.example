cat > .env.example << 'EOF'
# PostgreSQL
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# RabbitMQ
RABBITMQ_USER=
RABBITMQ_PASS=
EOF
```

**4. Garanta que o `.env` está no `.gitignore` raiz** — abra o arquivo `.gitignore` na raiz do monorepo e confirme que essa linha existe:
```
**/*.env